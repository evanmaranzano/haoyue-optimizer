VERSION = "3.4.0"

PRESETS = {
    "safe": "安全模式：仅低风险、可直接恢复的优化项（Store-safe 受保护）",
    "aggressive": "激进模式：包含游戏/隐私/高风险优化（Store-safe 始终保护）",
}

# ── Store-safe is enforced across ALL presets ──
STORE_SAFE_ENFORCED = True
